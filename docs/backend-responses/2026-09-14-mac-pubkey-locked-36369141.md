# Mac walk 36369141 — publickey denied, account locked (2026-09-14)

Stopped. One try. Did not complete.

hire 36369141-ab75-454e-bfad-5ccfb98d0717 paid txid 26d1a291ec5e976a… (pending: false).

poll delivered 02:59:54Z. Job JSON still had no host/key.

rental-access: host sovcompute.junction41.io port 40000 user renter pem rental-36369141.pem mode 600

ssh (one try, IdentitiesOnly=yes):

renter@sovcompute.junction41.io: Permission denied (publickey).

Banner OK (ED25519 host key). Key still refused. nvidia-smi not run. Did not reuse bf26bbb8 / 572847d0 / 764f781e.

## Orchard

Not a pem mismatch. Host `authorized_keys` matches `ssh-keygen -y` of the sealed key. Jail sshd: `User renter not allowed because account is locked` (`renter:!` in shadow, `UsePAM no`). Client reports that as Permission denied (publickey).

Unlocked live jail (`renter:*`). Seller TCP still held `143.110.214.97:40000`. Image rebuilt `06b36580de6b` with `usermod -p '*' renter` for the next jail.
