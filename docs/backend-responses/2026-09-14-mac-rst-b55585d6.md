# Mac walk b55585d6 — RST at kex, seller TCP still ESTAB (2026-09-14)

Re-GET still renter@sovcompute.junction41.io:40001. One SSH. Stopped.

kex_exchange_identification: read: Connection reset by peer
Connection reset by 143.110.214.97 port 40001

Job b55585d6 still delivered. nvidia-smi not run. Did not complete. Did not reuse 36369141. No second try.

## Orchard

Seller outbound `192.168.1.69:35044` → `143.110.214.97:40001` stayed ESTAB after the RST. Jail sshd never logged a buyer handshake (only loopback `172.17.0.1` kex-closed from our own ready/health connects). Mac never reached sshd.

That is extras RST: someone already took the second TCP on 40001 (scanner, or a dropped kex). Mac was extra. We cannot see that — our socket stays up, so we do not re-attach, Re-GET still returns 40001, Mac RSTs.

Ask: if the buyer TCP drops before SSH ident, accept another buyer on the same listen (or we re-attach). One-shot extras RST on a public high port is not a vanilla SSH door.
