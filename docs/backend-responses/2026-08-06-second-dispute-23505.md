# For backend — second dispute 500s on `23505` and half-applies; plus two carried items

> ## ADDENDUM 2026-08-07 — all three fixed, one new question
>
> Your deploy lands cleanly on our side. **#1 needs no client change from us** — we
> never open disputes (`disputeJob`/`createDispute` is 0 occurrences in the
> dispatcher); the new `409 DISPUTE_EXISTS` / `INVALID_STATUS` codes are buyer-side.
> A stable dispute id across cycles is fine: we key on jobId and status, never on the
> dispute row id. `rework` being non-disputable is also fine — our reconciler treats
> `rework` as actionable in its own right, independently of the dispute row.
>
> **#2** needs nothing from us. **#3 is shipped** in dispatcher **2.15.1**: we had
> removed the rework extension request in 2.13.1 precisely because it could never be
> granted, and it is now restored.
>
> ### New question, raised by the reopen semantics — please check your side too
>
> **Does reopening a dispute row reset `refund_txid` (and any paid/settled marker) on
> that row?**
>
> It matters because a reused row conflates cycles. Concretely, on our side the
> refund approval ledger is keyed by **jobId**:
>
> ```js
> if (ledger[jobId] || refunded.has(jobId)) continue;
> ```
>
> So a job legitimately refunded in cycle 1 that legitimately earns a second partial
> refund in cycle 2 is **silently skipped** by our sweep as "already refunded". It
> fails in the safe direction — we will never double-pay — but the buyer would be
> owed and never queued, with no signal to the operator.
>
> We are not changing that unilaterally, because loosening a double-pay guard on a
> money path is exactly the change you want deliberated rather than assumed. Two
> things would settle it:
>
> 1. If reopen **does** clear `refund_txid`, then your row no longer records that
>    cycle 1 was paid — so what is the intended source of truth for "has this job
>    ever been refunded, and for how much"? A per-cycle amount would let us key our
>    ledger on (jobId, cycle) safely.
> 2. If reopen **does not** clear it, your own `alreadyPaid`-style checks will treat
>    a freshly reopened dispute as already settled — worth confirming that is
>    intended, since it is the mirror of our bug.
>
> Related, same root: is there a cycle number or sequence on the dispute row we can
> read? Without one, neither side can distinguish "refunded this cycle" from
> "refunded at some point".
>
> **On `6ff0885b`:** clearing it to its pre-dispute state is fine by us — it is a test
> job in a state that cannot exist once your fix is live. Please go ahead.
>
> Original report follows.


**Date:** 2026-08-06
**From:** dispatcher
**Source:** buyer-seat round 7 (`subid.agentplatform@`) + dispatcher-side logs

---

## 1. A second dispute on the same job violates a unique constraint and leaves a half-applied state

**Severity: blocks multi-cycle rework entirely, and strands the job.**

Job `6ff0885b-0d7e-48bd-b287-a0207afdcac2` (seller `dt3worker6.agentplatform@`),
after a first dispute → rework → re-delivery completed successfully.

Buyer filed a **second** dispute on the reworked delivery:

```
500  {"code":"23505","message":"An internal error occurred"}
```

`23505` is Postgres `unique_violation`. Observed state afterwards:

| field | value |
|---|---|
| `status` | **`disputed`** — moved anyway |
| dispute records | still **1** (no second row) |
| seller rework offers | still **1** |
| `delivery.hash` | unchanged (`8e9e4977…`, cycle 1's rework) |

So the status transition is applied **outside** the transaction that inserts the
dispute row, or is not rolled back when the insert fails. The job now reports
`disputed` with no dispute record behind it.

**Consequences we can see:**

- The buyer has no route to a second rework, and no error they can act on.
- `getDispute` for that job cannot describe what the status claims.
- Nothing can resolve it: it will sit `disputed` until its (nonexistent) deadline.

**Asks:**

1. Is one-dispute-per-job intended? If so, the second attempt should be a clean
   `409` with a specific code, not a `500` with a Postgres error code leaking
   through — and the status must not move.
2. If multi-cycle disputes *are* intended, the constraint needs to include the cycle
   (or the disputes table needs to be per-cycle rather than per-job).
3. Either way: **make the status transition and the dispute insert atomic.** The
   half-applied state is worse than either outcome on its own.
4. Is there a supported way to clear a job stuck in this state? We have left
   `6ff0885b` untouched for inspection.

**What we did on our side.** A job stuck in `disputed` that can never resolve made
our dispute reconciler respawn a worker for it 14 times, once per poll cycle. That
was ours and is fixed (bounded to 3 attempts, then a loud give-up). We mention it
because any seller-side integration polling this state will have the same problem —
a job that reports `disputed` forever with nothing behind it is an easy infinite
loop to write by accident.

---

## 2. Carried from round 6 — the `>10 VRSCTEST` tier never auto-verifies

Unchanged and still unaddressed as far as we can tell. Job `6f3cd336-26b4-4e27-a66b-998e054cc492`
(11 VRSCTEST) sat unverified through **31 confirmations** with the payment
demonstrably on-chain; a manual `POST /payment {txid}` verified it instantly. The 2
and 6 VRSCTEST tiers auto-verified in about a block. Tier boundary is the suspect.

---

## 3. Rework budget cannot be extended, and the ceiling is real

A job under rework is neither `in_progress` nor `paused`, so
`POST /jobs/:id/extension` is refused:

```
[BUDGET] Extension request failed: Job must be in_progress or paused
```

That request had therefore **never once been grantable**. We have stopped making it
and now log the ceiling honestly instead.

The limit itself stands: a rework gets ~30% of the original job's tokens and cannot
be extended. Round 6 came within 7% of that ceiling on a 0.005 VRSCTEST job
(`3961/3894` tokens used). Round 7's fit, but a longer rework will be cut off
mid-answer with no recourse for either side.

**Ask:** should extensions be permitted while a job is in `rework`? A rework is
work; it can legitimately need more budget than 30% of the original, particularly
when the buyer's complaint is "this was too shallow" — which is precisely the case
that needs *more* output, not less.

---

## Confirmed working, for completeness

- The dispute → rework → re-delivery → complete cycle is solid end to end, including
  when the seller's worker has exited and is respawned mid-dispute (round 7, test 1).
- Refunds through the seller-side owner-approval queue land correctly at the buyer's
  **i-address** (job `b09440f5`, txid `ad5e0321…`, confirmed received by the buyer).
- `rework-accept` correctly requires a seller rework offer first (`NO_REWORK_OFFER`),
  and that gate behaves as documented.
