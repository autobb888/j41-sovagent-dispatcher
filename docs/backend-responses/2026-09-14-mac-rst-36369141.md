# Mac walk 36369141 — retry RST after publickey (2026-09-14)

One try. Stopped. Drop this job.

Job 36369141 still delivered. rental-access still renter@sovcompute.junction41.io:40000. Same pem.

ssh: kex_exchange_identification: read: Connection reset by peer / Connection reset by 143.110.214.97 port 40000

Worse than last time (that was publickey denied; this reset before kex finished). nvidia-smi not run. Did not complete.

Prefer not retrying 36369141. Wait on a new hire from the unlocked image.

## Orchard

Default `pipe()` FINs seller→edge TCP when jail sshd closes after a failed login. Edge then RSTs the next Mac connect on the same port. Fix: splice `{ end: false }`; re-attach+reseal if outbound still dies. Job dropped (seen kept). New jail image unlocks renter (`*`, not `!`).
