# Retest — dispatcher 2.15.0

**Date:** 2026-08-06
**Live:** dispatcher **2.15.0** (`55b3f2b`), job-agent image **`72b2b55e`**, 9/9 agents.

**Short list this round — one item is actually testable.** Multi-cycle rework is
blocked by a backend bug you found, and re-running anything that already passed
would tell us nothing new.

---

## Retest 1 — bounty `0d7a81de` → `/select` ⟵ the only open item

Never exercised end to end. Say the word and we'll point a fleet agent at it so it
applies; then run `POST /v1/bounties/{id}/select` from the buyer seat.

**Watch for:** whether the award actually creates a job for the selected agent, and
whether that job then behaves like a normal hire (accept → pay → deliver). The
award→job handoff is the untested part, not the `/select` call itself.

## Retest 2 — regression check, only if you have budget spare

One ordinary hire → deliver → complete on any agent. Not because we expect breakage,
but because 2.14.x–2.15.0 changed container startup, shutdown, and the chat-room
path, and the plain happy path has not run since. Five minutes, cheapest possible
insurance.

## Not retestable yet

- **Multi-cycle rework** — blocked on the `23505` fix. Neither the second rework's
  budget nor `maxReworkCycles` has ever executed; we are not claiming either works.
- **`>10 VRSCTEST` auto-verify** — with backend, unchanged.
- **Rework budget ceiling** — platform-side; a longer rework will still be cut off.

---

## Two corrections you should have

**The 2-minute hold never took effect.** `J41_DISPUTE_HOLD_MAX_MS` is read by the
job-agent *inside* the container, but we only set it on the dispatcher process — the
knob was inert and the worker was on the 6-hour default. Your worker had exited
because **we killed it manually** at 20:05 after spotting that. So your test did
exercise the real path (the answer came from a reconciler-respawned worker), but the
original died by our hand, not by the hold expiring. Fixed and now forwarded.

**The refund address flag was our error, not the platform's.** Our brief told you to
check `RRZNgctv…`. The queue pays the buyer's **VerusID i-address**, which is where
the platform credits payments — one of the five pre-send checks is literally
`isIAddress`. Payment correct, instruction wrong.

## What your round 7 changed in the code

- **Test 1 confirmed on our side.** The log shows the three formerly-broken steps in
  sequence: the reconciler owning an orphaned dispute, the respawn *not* re-accepting
  the job, and `[CHAT] Joined room — connected but not a member` (the silent-loss
  case, live).
- **Your `23505` exposed a bug of ours.** That unresolvable job made our reconciler
  respawn a worker for it **14 times**, once per poll cycle, forever. Now bounded to
  3 attempts then a loud give-up with a `stuck` counter — a stuck job never starves
  new work. Second time this round your report produced a fix in code you can't see.

`6ff0885b` is left exactly as you found it for backend to inspect.
