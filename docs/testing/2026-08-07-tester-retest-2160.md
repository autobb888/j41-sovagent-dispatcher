# Retest — dispatcher 2.16.0

**Date:** 2026-08-07
**Live:** dispatcher **2.16.0** (`1e5983e`), job-agent image **`641d2367`**, 9/9 agents.

Round 8 passed all three retests. **One item to re-run**, plus one correction that
changes what your cycle-2 result meant.

---

## Correction — extensions WERE requested, in both cycles. You could not see them.

You reported cycle 2 as *"no extension needed"*. Our worker log says otherwise:

```
cycle 1:  Rework token budget at  90% (2379/2645) — requesting extension
          [BUDGET] Extension requested: 0.001267 VRSC for ~1322 tokens
cycle 2:  Rework token budget at 113% (4732/4178) — requesting extension
          [BUDGET] Extension request failed: A budget request is already pending
```

So cycle 1 raised a real 0.001267 VRSC request, and cycle 2 needed one and was
refused because cycle 1's was still pending. Then we queried the platform as the
seller:

```
getExtensions(538c039b…)  →  0 extensions
```

**Zero.** The request exists well enough to block every later one, but is not
returned by the extensions endpoint — so neither you nor we can approve or pay it,
and it can never clear. Your reading was correct for what you could see; the request
was invisible to both seats. It is with backend as R8-1.

Two consequences worth knowing for future runs: cycle 2 ran to **113%** of its budget
and delivered anyway (the agent absorbed the overrun), and **if a rework ever needs
more budget, the first ask consumes the mechanism permanently** until backend fixes
it. So treat "no extension prompt appeared" as *unproven*, not as *not needed*.

---

## Retest 1 — the over-limit dispute dead-end, now that our half is fixed ⟵ the only re-run

Your finding: a third dispute and its `rework-accept` both return `200`, the status
flips to `rework`, and nothing ever delivers — the job dead-ends.

**Both halves of that were ours**, and both are fixed in 2.16.0:

- the worker declined cycle 3 with a bare log line and told nobody; it now posts a
  chat message to you explaining the policy limit, and raises an `ACTION NEEDED`
  alarm to the operator; and
- `respond-dispute` let the operator offer a third rework in the first place. It now
  reads the agent's on-chain `maxReworkCycles` and refuses.

**Please re-run it:** hire Shreck → deliver → dispute → accept rework (cycle 1) →
dispute → accept rework (cycle 2) → **dispute a third time**.

Expected now: we will **not** make a third rework offer, so your `rework-accept`
should fail with **`NO_REWORK_OFFER`** rather than returning `200` and dead-ending.
You should also receive **a chat message** from the agent saying it cannot take a
third rework and that the operator will respond to the dispute directly.

**Report as a failure if:** the third `rework-accept` returns `200` again, or the job
sits in `rework` with no chat message. Either means our fix missed.

*(Note this is why the cycle counter mattered: it used to live only inside the worker
container and reset if that worker was replaced. It is now recorded seller-side, so a
respawn between cycles can no longer quietly re-grant you a cycle.)*

## Nothing else to re-run

Bounty `/select`, the award→job handoff, cycles 1–2, and the happy-path regression all
passed and nothing in 2.16.0 touches those paths. Your four bounty flags (null
`payment.address`, 16-hex `jobHash`, no loser notification, count-only award
signature) are backend's and are passed through as R8-3 — no action from you.

## Housekeeping

`538c039b` is being cleared on our side: it is stuck in `rework` from the cycle-3
dead-end, with a **seller-owned deadline**, so left alone it would auto-default our
agent for a state the old bug created. If you want it preserved for backend to
inspect, say so now and we will leave it.
